name: Ping Streamlit App

on:
  schedule:
    - cron: "0 */2 * * *"  # Every 2 hours
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App using curl
        run: curl -s https://portfoliowebsite-deven.streamlit.app
